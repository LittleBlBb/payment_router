spring:
  application:
    name: payment-router

  datasource:
    url: jdbc:postgresql://localhost:5432/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate

  MOCK_GATEWAY_MAX_AMOUNT: ${MOCK_GATEWAY_MAX_AMOUNT}