spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_router
    username: payment_router
    password: payment_router

  jpa:
    hibernate:
      ddl-auto: validate

mock:
  gateway:
    max-amount: ${MOCK_GATEWAY_MAX_AMOUNT:500000.00}